apiVersion: v1
kind: Service
metadata:
  name: flowershop
spec:
  selector:
    app: flowershop
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: {{ .Values.service.type }}
